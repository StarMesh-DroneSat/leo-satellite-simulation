function newSatLoc() {
    function newsat1() {
       var newrandx = Math.floor(Math.random()*215) + 0; // this will get a number between 1 and 99;
       newrandx *= Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases
       var newrandy = Math.floor(Math.random()*100) + 0; // this will get a number between 1 and 99;
       newrandy *=  Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases        
       sat1.position.x, sat1.position.y = newrandx, newrandy;         
    }       
    function newsat2() {
       var newrandx = Math.floor(Math.random()*215) + 0; // this will get a number between 1 and 99;
       newrandx *= Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases
       var newrandy = Math.floor(Math.random()*100) + 0; // this will get a number between 1 and 99;
       newrandy *=  Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases        
       sat2.position.x, sat2.position.y = newrandx, newrandy;         
    }          
    function newsat3() {
       var newrandx = Math.floor(Math.random()*215) + 0; // this will get a number between 1 and 99;
       newrandx *= Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases
       var newrandy = Math.floor(Math.random()*100) + 0; // this will get a number between 1 and 99;
       newrandy *=  Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases        
       sat3.position.x, sat3.position.y = newrandx, newrandy;         
    }   
    function newsat4() {
       var newrandx = Math.floor(Math.random()*215) + 0; // this will get a number between 1 and 99;
       newrandx *= Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases
       var newrandy = Math.floor(Math.random()*100) + 0; // this will get a number between 1 and 99;
       newrandy *=  Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases        
       sat4.position.x, sat4.position.y = newrandx, newrandy;         
    }          
    function newsat5() {
       var newrandx = Math.floor(Math.random()*215) + 0; // this will get a number between 1 and 99;
       newrandx *= Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases
       var newrandy = Math.floor(Math.random()*100) + 0; // this will get a number between 1 and 99;
       newrandy *=  Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases        
       sat5.position.x, sat5.position.y = newrandx, newrandy;         
    };           
    function newsat6() {
       var newrandx = Math.floor(Math.random()*215) + 0; // this will get a number between 1 and 99;
       newrandx *= Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases
       var newrandy = Math.floor(Math.random()*100) + 0; // this will get a number between 1 and 99;
       newrandy *=  Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases        
       sat6.position.x, sat6.position.y = newrandx, newrandy;         
    };           
    function newsat7() {
       var newrandx = Math.floor(Math.random()*215) + 0; // this will get a number between 1 and 99;
       newrandx *= Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases
       var newrandy = Math.floor(Math.random()*100) + 0; // this will get a number between 1 and 99;
       newrandy *=  Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases        
       sat7.position.x, sat7.position.y = newrandx, newrandy;         
    };           
    function newsat8() {
       var newrandx = Math.floor(Math.random()*215) + 0; // this will get a number between 1 and 99;
       newrandx *= Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases
       var newrandy = Math.floor(Math.random()*100) + 0; // this will get a number between 1 and 99;
       newrandy *=  Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases        
       sat8.position.x, sat8.position.y = newrandx, newrandy;         
    };           
    function newsat9() {
       var newrandx = Math.floor(Math.random()*215) + 0; // this will get a number between 1 and 99;
       newrandx *= Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases
       var newrandy = Math.floor(Math.random()*100) + 0; // this will get a number between 1 and 99;
       newrandy *=  Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases        
       sat9.position.x, sat9.position.y = newrandx, newrandy;         
    };           
    function newsat10() {
       var newrandx = Math.floor(Math.random()*215) + 0; // this will get a number between 1 and 99;
       newrandx *= Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases
       var newrandy = Math.floor(Math.random()*100) + 0; // this will get a number between 1 and 99;
       newrandy *=  Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases        
       sat10.position.x, sat10.position.y = newrandx, newrandy;         
    };           
    function newsat11() {
       var newrandx = Math.floor(Math.random()*215) + 0; // this will get a number between 1 and 99;
       newrandx *= Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases
       var newrandy = Math.floor(Math.random()*100) + 0; // this will get a number between 1 and 99;
       newrandy *=  Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases        
       sat11.position.x, sat11.position.y = newrandx, newrandy;         
    };           
    function newsat12() {
       var newrandx = Math.floor(Math.random()*215) + 0; // this will get a number between 1 and 99;
       newrandx *= Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases
       var newrandy = Math.floor(Math.random()*100) + 0; // this will get a number between 1 and 99;
       newrandy *=  Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases        
       sat12.position.x, sat12.position.y = newrandx, newrandy;         
    };           
    function newsat13() {
       var newrandx = Math.floor(Math.random()*215) + 0; // this will get a number between 1 and 99;
       newrandx *= Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases
       var newrandy = Math.floor(Math.random()*100) + 0; // this will get a number between 1 and 99;
       newrandy *=  Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases        
       sat13.position.x, sat13.position.y = newrandx, newrandy;         
    };           
    function newsat14() {
       var newrandx = Math.floor(Math.random()*215) + 0; // this will get a number between 1 and 99;
       newrandx *= Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases
       var newrandy = Math.floor(Math.random()*100) + 0; // this will get a number between 1 and 99;
       newrandy *=  Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases        
       sat14.position.x, sat14.position.y = newrandx, newrandy;         
    };
    function newsat15() {
       var newrandx = Math.floor(Math.random()*215) + 0; // this will get a number between 1 and 99;
       newrandx *= Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases
       var newrandy = Math.floor(Math.random()*100) + 0; // this will get a number between 1 and 99;
       newrandy *=  Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases        
       sat15.position.x, sat15.position.y = newrandx, newrandy;         
    };  
    function newsat16() {
       var newrandx = Math.floor(Math.random()*215) + 0; // this will get a number between 1 and 99;
       newrandx *= Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases
       var newrandy = Math.floor(Math.random()*100) + 0; // this will get a number between 1 and 99;
       newrandy *=  Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases        
       sat16.position.x, sat16.position.y = newrandx, newrandy;         
    };
    function newsat17() {
       var newrandx = Math.floor(Math.random()*215) + 0; // this will get a number between 1 and 99;
       newrandx *= Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases
       var newrandy = Math.floor(Math.random()*100) + 0; // this will get a number between 1 and 99;
       newrandy *=  Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases        
       sat17.position.x, sat17.position.y = newrandx, newrandy;         
    };
    function newsat18() {
       var newrandx = Math.floor(Math.random()*215) + 0; // this will get a number between 1 and 99;
       newrandx *= Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases
       var newrandy = Math.floor(Math.random()*100) + 0; // this will get a number between 1 and 99;
       newrandy *=  Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases        
       sat18.position.x, sat18.position.y = newrandx, newrandy;         
    };
    function newsat19() {
       var newrandx = Math.floor(Math.random()*215) + 0; // this will get a number between 1 and 99;
       newrandx *= Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases
       var newrandy = Math.floor(Math.random()*100) + 0; // this will get a number between 1 and 99;
       newrandy *=  Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases        
       sat19.position.x, sat19.position.y = newrandx, newrandy;         
    };
    function newsat20() {
       var newrandx = Math.floor(Math.random()*215) + 0; // this will get a number between 1 and 99;
       newrandx *= Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases
       var newrandy = Math.floor(Math.random()*100) + 0; // this will get a number between 1 and 99;
       newrandy *=  Math.floor(Math.random()*2) == 1 ? 1 : -1; // this will add minus sign in 50% of cases        
       sat20.position.x, sat20.position.y = newrandx, newrandy;         
    }; 
    newsat1();
    newsat2();
    newsat3();
    newsat4();
    newsat5();
    newsat6();
    newsat7();
    newsat8();
    newsat9();
    newsat10();
    newsat11();
    newsat12();
    newsat13();
    newsat14();
    newsat15();           
    newsat16();
    newsat17();
    newsat18();
    newsat19();
    newsat20();           
}  

function makeNewLoc() {
   setInterval(function() { newSatLoc(); }, 360000);
  //  Refresh the Satellite Location every 10 minutes
   window.alert('This Simulation is Optimzed for "Web" Viewing Only!');
}

makeNewLoc();
