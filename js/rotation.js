function rotate_all_satellites() {
    sat1.rotation.x += rot1x;
            sat1.rotation.y -= rot1y;
            sat1.rotation.z += rot1z;
            sat2.rotation.x -= rot2x;
            sat2.rotation.y += rot2y;
            sat2.rotation.z -= rot2z;


            sat3.rotation.x += rot3x;
            sat3.rotation.y -= rot3y;
            sat3.rotation.z += rot3z;

            sat4.rotation.x -= rot4x;
            sat4.rotation.y += rot4y;
            sat4.rotation.z -= rot4z;

            sat5.rotation.x += rot5x;
            sat5.rotation.y -= rot5y;
            sat5.rotation.z += rot5z;

            sat6.rotation.x -= rot6x;
            sat6.rotation.y += rot6y;
            sat6.rotation.z -= rot6z;


            sat7.rotation.x += rot7x;
            sat7.rotation.y -= rot7y;
            sat7.rotation.z += rot7z;

            sat8.rotation.x -= rot8x;
            sat8.rotation.y += rot8y;
            sat8.rotation.z += rot8z;


            sat9.rotation.x -= rot9x;
            sat9.rotation.y += rot9y;
            sat9.rotation.z += rot9z;

            sat10.rotation.x -= rot10x;
            sat10.rotation.y += rot10y;
            sat10.rotation.z -= rot10z;

            sat11.rotation.x += rot11x;
            sat11.rotation.y -= rot11y;
            sat11.rotation.z += rot11z;


            sat12.rotation.x -= rot12x;
            sat12.rotation.y += rot12y;
            sat12.rotation.z -= rot12z;

            sat13.rotation.x += rot13x;
            sat13.rotation.y -= rot13y;
            sat13.rotation.z += rot13z;

            sat14.rotation.x -= rot14x;
            sat14.rotation.y += rot14y;
            sat14.rotation.z -= rot14z;

            sat15.rotation.x += rot15x;
            sat15.rotation.y -= rot15y;
            sat15.rotation.z += rot15z;

            sat16.rotation.x -= rot16x;
            sat16.rotation.y += rot16y;
            sat16.rotation.z -= rot16z;

            sat17.rotation.x += rot17x;
            sat17.rotation.y -= rot17y;
            sat17.rotation.z += rot17z;

            sat18.rotation.x -= rot18x;
            sat18.rotation.y += rot18y;
            sat18.rotation.z -= rot18z;


            sat19.rotation.x -= rot19x;
            sat19.rotation.y -= rot19y;
            sat19.rotation.z += rot19z;

            sat20.rotation.x += rot20x;
            sat20.rotation.y -= rot20y;
            sat20.rotation.z -= rot20z;

            sat21.rotation.x += rot21x;
            sat21.rotation.y -= rot21y;
            sat21.rotation.z += rot21z;

            sat22.rotation.x += rot22x;
            sat22.rotation.y += rot22y;
            sat22.rotation.z -= rot22z;

            sat23.rotation.x += rot23x;
            sat23.rotation.y -= rot23y;
            sat23.rotation.z -= rot23z;

            sat24.rotation.x += rot24x;
            sat24.rotation.y -= rot24y;
            sat24.rotation.z -= rot24z;

            sat25.rotation.x += rot25x;
            sat25.rotation.y -= rot25y;
            sat25.rotation.z -= rot25z;

            sat26.rotation.x += rot26x;
            sat26.rotation.y -= rot26y;
            sat26.rotation.z -= rot26z;

            sat27.rotation.x += rot27x;
            sat27.rotation.y -= rot27y;
            sat27.rotation.z += rot27z;

            sat28.rotation.x -= rot28x;
            sat28.rotation.y -= rot28y;
            sat28.rotation.z += rot28z;

            sat29.rotation.x += rot29x;
            sat29.rotation.y -= rot29y;
            sat29.rotation.z += rot29z;

            sat30.rotation.x -= rot30x;
            sat30.rotation.y -= rot30y;
            sat30.rotation.z += rot30z;

}