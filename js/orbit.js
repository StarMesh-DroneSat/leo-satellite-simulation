function orbit_all_satellites() {
    
            sat1.position.x += pos1x;
            sat1.position.y -= pos1y;
            sat2.position.x -= pos2x;
            sat2.position.y += pos2y;
            sat3.position.x += pos3x;
            sat3.position.y -= pos3y;
            sat4.position.x -= pos4x;
            sat4.position.y += pos4y;
            sat5.position.x -= pos5x;
            sat5.position.y += pos5y;
            sat6.position.x += pos6x;
            sat6.position.y -= pos6y;
            sat7.position.x += pos7x;
            sat7.position.y -= pos7y;
            sat8.position.x += pos8x;
            sat8.position.y -= pos8y;
            sat9.position.x += pos9x;
            sat9.position.y += pos9y;
            sat10.position.x -= pos10x;
            sat10.position.y += pos10y;
            sat11.position.x += pos11x;
            sat11.position.y -= pos11y;
            sat12.position.x += pos12x;
            sat12.position.y -= pos12y;
            sat13.position.x += pos13x;
            sat13.position.y += pos13y;
            sat14.position.x -= pos14x;
            sat14.position.y += pos14y;
            sat15.position.x += pos15x;
            sat15.position.y -= pos15y;
            sat16.position.x += pos16x;
            sat16.position.y += pos16y;
            sat17.position.x -= pos17x;
            sat17.position.y -= pos17y;
            sat18.position.x -= pos18x;
            sat18.position.y += pos18y;
            sat19.position.x -= pos19x;
            sat19.position.y -= pos19y;
            sat20.position.x += pos20x;
            sat20.position.y += pos20y;
            sat21.position.x -= pos21x;
            sat21.position.y -= pos21y;
            sat22.position.x += pos22x;
            sat22.position.y += pos22y;
            sat23.position.x -= pos23x;
            sat23.position.y -= pos23y;
            sat24.position.x += pos24x;
            sat24.position.y -= pos24y;
            sat25.position.x += pos25x;
            sat25.position.y -= pos25y;
            sat26.position.x += pos26x;
            sat26.position.y += pos26y;
            sat27.position.x += pos27x;
            sat27.position.y += pos27y;
            sat28.position.x -= pos28x;
            sat28.position.y += pos28y;
            sat29.position.x += pos29x;
            sat29.position.y -= pos29y;
            sat30.position.x += pos30x;
            sat30.position.y += pos30y;

            sat1i.position.x += pos1x;
            sat1i.position.y -= pos1y;

            sat2i.position.x -= pos2x;
            sat2i.position.y += pos2y;

            sat3i.position.x += pos3x;
            sat3i.position.y -= pos3y;

            sat4i.position.x -= pos4x;
            sat4i.position.y += pos4y;

            sat5i.position.x -= pos5x;
            sat5i.position.y += pos5y;

            sat6i.position.x += pos6x;
            sat6i.position.y -= pos6y;

            sat7i.position.x += pos7x;
            sat7i.position.y -= pos7y;

            sat8i.position.x += pos8x;
            sat8i.position.y -= pos8y;

            sat9i.position.x += pos9x;
            sat9i.position.y += pos9y;

            sat10i.position.x -= pos10x;
            sat10i.position.y += pos10y;

            sat11i.position.x += pos11x;
            sat11i.position.y -= pos11y;

            sat12i.position.x += pos12x;
            sat12i.position.y -= pos12y;

            sat13i.position.x += pos13x;
            sat13i.position.y += pos13y;

            sat14i.position.x -= pos14x;
            sat14i.position.y += pos14y;

            sat15i.position.x += pos15x;
            sat15i.position.y -= pos15y;

            sat16i.position.x += pos16x;
            sat16i.position.y += pos16y;
            
            sat17i.position.x -= pos17x;
            sat17i.position.y -= pos17y;

            sat18i.position.x -= pos18x;
            sat18i.position.y += pos18y;

            sat19i.position.x -= pos19x;
            sat19i.position.y -= pos19y;

            sat20i.position.x += pos20x;
            sat20i.position.y += pos20y;

            sat21i.position.x -= pos21x;
            sat21i.position.y -= pos21y;

            sat22i.position.x += pos22x;
            sat22i.position.y += pos22y;

            sat23i.position.x -= pos23x;
            sat23i.position.y -= pos23y;

            sat24i.position.x += pos24x;
            sat24i.position.y -= pos24y;

            sat25i.position.x += pos25x;
            sat25i.position.y -= pos25y;

            sat26i.position.x += pos26x;
            sat26i.position.y += pos26y;

            sat27i.position.x += pos27x;
            sat27i.position.y += pos27y;

            sat28i.position.x -= pos28x;
            sat28i.position.y += pos28y;

            sat29i.position.x += pos29x;
            sat29i.position.y -= pos29y;

            sat30i.position.x += pos30x;
            sat30i.position.y += pos30y;

            sat1ii.position.x += pos1x;
            sat1ii.position.y -= pos1y;

            sat2ii.position.x -= pos2x;
            sat2ii.position.y += pos2y;

            sat3ii.position.x += pos3x;
            sat3ii.position.y -= pos3y;

            sat4ii.position.x -= pos4x;
            sat4ii.position.y += pos4y;

            sat5ii.position.x -= pos5x;
            sat5ii.position.y += pos5y;

            sat6ii.position.x += pos6x;
            sat6ii.position.y -= pos6y;

            sat7ii.position.x += pos7x;
            sat7ii.position.y -= pos7y;

            sat8ii.position.x += pos8x;
            sat8ii.position.y -= pos8y;

            sat9ii.position.x += pos9x;
            sat9ii.position.y += pos9y;

            sat10ii.position.x -= pos10x;
            sat10ii.position.y += pos10y;

            sat11ii.position.x += pos11x;
            sat11ii.position.y -= pos11y;

            sat12ii.position.x += pos12x;
            sat12ii.position.y -= pos12y;

            sat13ii.position.x += pos13x;
            sat13ii.position.y += pos13y;

            sat14ii.position.x -= pos14x;
            sat14ii.position.y += pos14y;

            sat15ii.position.x += pos15x;
            sat15ii.position.y -= pos15y;

            sat16ii.position.x += pos16x;
            sat16ii.position.y += pos16y;
            
            sat17ii.position.x -= pos17x;
            sat17ii.position.y -= pos17y;

            sat18ii.position.x -= pos18x;
            sat18ii.position.y += pos18y;

            sat19ii.position.x -= pos19x;
            sat19ii.position.y -= pos19y;

            sat20ii.position.x += pos20x;
            sat20ii.position.y += pos20y;

            sat21ii.position.x -= pos21x;
            sat21ii.position.y -= pos21y;

            sat22ii.position.x += pos22x;
            sat22ii.position.y += pos22y;

            sat23ii.position.x -= pos23x;
            sat23ii.position.y -= pos23y;

            sat24ii.position.x += pos24x;
            sat24ii.position.y -= pos24y;

            sat25ii.position.x += pos25x;
            sat25ii.position.y -= pos25y;

            sat26ii.position.x += pos26x;
            sat26ii.position.y += pos26y;

            sat27ii.position.x += pos27x;
            sat27ii.position.y += pos27y;

            sat28ii.position.x -= pos28x;
            sat28ii.position.y += pos28y;

            sat29ii.position.x += pos29x;
            sat29ii.position.y -= pos29y;

            sat30ii.position.x += pos30x;
            sat30ii.position.y += pos30y;

            sat1iii.position.x += pos1x;
            sat1iii.position.y -= pos1y;

            sat2iii.position.x -= pos2x;
            sat2iii.position.y += pos2y;

            sat3iii.position.x += pos3x;
            sat3iii.position.y -= pos3y;

            sat4iii.position.x -= pos4x;
            sat4iii.position.y += pos4y;

            sat5iii.position.x -= pos5x;
            sat5iii.position.y += pos5y;

            sat6iii.position.x += pos6x;
            sat6iii.position.y -= pos6y;

            sat7iii.position.x += pos7x;
            sat7iii.position.y -= pos7y;

            sat8iii.position.x += pos8x;
            sat8iii.position.y -= pos8y;

            sat9iii.position.x += pos9x;
            sat9iii.position.y += pos9y;

            sat10iii.position.x -= pos10x;
            sat10iii.position.y += pos10y;

            sat11iii.position.x += pos11x;
            sat11iii.position.y -= pos11y;

            sat12iii.position.x += pos12x;
            sat12iii.position.y -= pos12y;

            sat13iii.position.x += pos13x;
            sat13iii.position.y += pos13y;

            sat14iii.position.x -= pos14x;
            sat14iii.position.y += pos14y;

            sat15iii.position.x += pos15x;
            sat15iii.position.y -= pos15y;

            sat16iii.position.x += pos16x;
            sat16iii.position.y += pos16y;
            
            sat17iii.position.x -= pos17x;
            sat17iii.position.y -= pos17y;

            sat18iii.position.x -= pos18x;
            sat18iii.position.y += pos18y;

            sat19iii.position.x -= pos19x;
            sat19iii.position.y -= pos19y;

            sat20iii.position.x += pos20x;
            sat20iii.position.y += pos20y;

            sat21iii.position.x -= pos21x;
            sat21iii.position.y -= pos21y;

            sat22iii.position.x += pos22x;
            sat22iii.position.y += pos22y;

            sat23iii.position.x -= pos23x;
            sat23iii.position.y -= pos23y;

            sat24iii.position.x += pos24x;
            sat24iii.position.y -= pos24y;

            sat25iii.position.x += pos25x;
            sat25iii.position.y -= pos25y;

            sat26iii.position.x += pos26x;
            sat26iii.position.y += pos26y;

            sat27iii.position.x += pos27x;
            sat27iii.position.y += pos27y;

            sat28iii.position.x -= pos28x;
            sat28iii.position.y += pos28y;

            sat29iii.position.x += pos29x;
            sat29iii.position.y -= pos29y;

            sat30iii.position.x += pos30x;
            sat30iii.position.y += pos30y;

            sat1iv.position.x += pos1x;
            sat1iv.position.y -= pos1y;

            sat2iv.position.x -= pos2x;
            sat2iv.position.y += pos2y;

            sat3iv.position.x += pos3x;
            sat3iv.position.y -= pos3y;

            sat4iv.position.x -= pos4x;
            sat4iv.position.y += pos4y;

            sat5iv.position.x -= pos5x;
            sat5iv.position.y += pos5y;

            sat6iv.position.x += pos6x;
            sat6iv.position.y -= pos6y;

            sat7iv.position.x += pos7x;
            sat7iv.position.y -= pos7y;

            sat8iv.position.x += pos8x;
            sat8iv.position.y -= pos8y;

            sat9iv.position.x += pos9x;
            sat9iv.position.y += pos9y;

            sat10iv.position.x -= pos10x;
            sat10iv.position.y += pos10y;

            sat11iv.position.x += pos11x;
            sat11iv.position.y -= pos11y;

            sat12iv.position.x += pos12x;
            sat12iv.position.y -= pos12y;

            sat13iv.position.x += pos13x;
            sat13iv.position.y += pos13y;

            sat14iv.position.x -= pos14x;
            sat14iv.position.y += pos14y;

            sat15iv.position.x += pos15x;
            sat15iv.position.y -= pos15y;

            sat16iv.position.x += pos16x;
            sat16iv.position.y += pos16y;
            
            sat17iv.position.x -= pos17x;
            sat17iv.position.y -= pos17y;

            sat18iv.position.x -= pos18x;
            sat18iv.position.y += pos18y;

            sat19iv.position.x -= pos19x;
            sat19iv.position.y -= pos19y;

            sat20iv.position.x += pos20x;
            sat20iv.position.y += pos20y;

            sat21iv.position.x -= pos21x;
            sat21iv.position.y -= pos21y;

            sat22iv.position.x += pos22x;
            sat22iv.position.y += pos22y;

            sat23iv.position.x -= pos23x;
            sat23iv.position.y -= pos23y;

            sat24iv.position.x += pos24x;
            sat24iv.position.y -= pos24y;

            sat25iv.position.x += pos25x;
            sat25iv.position.y -= pos25y;

            sat26iv.position.x += pos26x;
            sat26iv.position.y += pos26y;

            sat27iv.position.x += pos27x;
            sat27iv.position.y += pos27y;

            sat28iv.position.x -= pos28x;
            sat28iv.position.y += pos28y;

            sat29iv.position.x += pos29x;
            sat29iv.position.y -= pos29y;

            sat30iv.position.x += pos30x;
            sat30iv.position.y += pos30y;


            sat1v.position.x += pos1x;
            sat1v.position.y -= pos1y;

            sat2v.position.x -= pos2x;
            sat2v.position.y += pos2y;

            sat3v.position.x += pos3x;
            sat3v.position.y -= pos3y;

            sat4v.position.x -= pos4x;
            sat4v.position.y += pos4y;

            sat5v.position.x -= pos5x;
            sat5v.position.y += pos5y;

            sat6v.position.x += pos6x;
            sat6v.position.y -= pos6y;

            sat7v.position.x += pos7x;
            sat7v.position.y -= pos7y;

            sat8v.position.x += pos8x;
            sat8v.position.y -= pos8y;

            sat9v.position.x += pos9x;
            sat9v.position.y += pos9y;

            sat10v.position.x -= pos10x;
            sat10v.position.y += pos10y;

            sat11v.position.x += pos11x;
            sat11v.position.y -= pos11y;

            sat12v.position.x += pos12x;
            sat12v.position.y -= pos12y;

            sat13v.position.x += pos13x;
            sat13v.position.y += pos13y;

            sat14v.position.x -= pos14x;
            sat14v.position.y += pos14y;

            sat15v.position.x += pos15x;
            sat15v.position.y -= pos15y;

            sat16v.position.x += pos16x;
            sat16v.position.y += pos16y;
            
            sat17v.position.x -= pos17x;
            sat17v.position.y -= pos17y;

            sat18v.position.x -= pos18x;
            sat18v.position.y += pos18y;

            sat19v.position.x -= pos19x;
            sat19v.position.y -= pos19y;

            sat20v.position.x += pos20x;
            sat20v.position.y += pos20y;

            sat21v.position.x -= pos21x;
            sat21v.position.y -= pos21y;

            sat22v.position.x += pos22x;
            sat22v.position.y += pos22y;

            sat23v.position.x -= pos23x;
            sat23v.position.y -= pos23y;

            sat24v.position.x += pos24x;
            sat24v.position.y -= pos24y;

            sat25v.position.x += pos25x;
            sat25v.position.y -= pos25y;

            sat26v.position.x += pos26x;
            sat26v.position.y += pos26y;

            sat27v.position.x += pos27x;
            sat27v.position.y += pos27y;

            sat28v.position.x -= pos28x;
            sat28v.position.y += pos28y;

            sat29v.position.x += pos29x;
            sat29v.position.y -= pos29y;

            sat30v.position.x += pos30x;
            sat30v.position.y += pos30y;


            sat1vi.position.x += pos1x;
            sat1vi.position.y -= pos1y;

            sat2vi.position.x -= pos2x;
            sat2vi.position.y += pos2y;

            sat3vi.position.x += pos3x;
            sat3vi.position.y -= pos3y;

            sat4vi.position.x -= pos4x;
            sat4vi.position.y += pos4y;

            sat5vi.position.x -= pos5x;
            sat5vi.position.y += pos5y;

            sat6vi.position.x += pos6x;
            sat6vi.position.y -= pos6y;

            sat7vi.position.x += pos7x;
            sat7vi.position.y -= pos7y;

            sat8vi.position.x += pos8x;
            sat8vi.position.y -= pos8y;

            sat9vi.position.x += pos9x;
            sat9vi.position.y += pos9y;

            sat10vi.position.x -= pos10x;
            sat10vi.position.y += pos10y;

            sat11vi.position.x += pos11x;
            sat11vi.position.y -= pos11y;

            sat12vi.position.x += pos12x;
            sat12vi.position.y -= pos12y;

            sat13vi.position.x += pos13x;
            sat13vi.position.y += pos13y;

            sat14vi.position.x -= pos14x;
            sat14vi.position.y += pos14y;

            sat15vi.position.x += pos15x;
            sat15vi.position.y -= pos15y;

            sat16vi.position.x += pos16x;
            sat16vi.position.y += pos16y;
            
            sat17vi.position.x -= pos17x;
            sat17vi.position.y -= pos17y;

            sat18vi.position.x -= pos18x;
            sat18vi.position.y += pos18y;

            sat19vi.position.x -= pos19x;
            sat19vi.position.y -= pos19y;

            sat20vi.position.x += pos20x;
            sat20vi.position.y += pos20y;

            sat21vi.position.x -= pos21x;
            sat21vi.position.y -= pos21y;

            sat22vi.position.x += pos22x;
            sat22vi.position.y += pos22y;

            sat23vi.position.x -= pos23x;
            sat23vi.position.y -= pos23y;

            sat24vi.position.x += pos24x;
            sat24vi.position.y -= pos24y;

            sat25vi.position.x += pos25x;
            sat25vi.position.y -= pos25y;

            sat26vi.position.x += pos26x;
            sat26vi.position.y += pos26y;

            sat27vi.position.x += pos27x;
            sat27vi.position.y += pos27y;

            sat28vi.position.x -= pos28x;
            sat28vi.position.y += pos28y;

            sat29vi.position.x += pos29x;
            sat29vi.position.y -= pos29y;

            sat30vi.position.x += pos30x;
            sat30vi.position.y += pos30y;

            sat1vii.position.x += pos1x;
            sat1vii.position.y -= pos1y;

            sat2vii.position.x -= pos2x;
            sat2vii.position.y += pos2y;

            sat3vii.position.x += pos3x;
            sat3vii.position.y -= pos3y;

            sat4vii.position.x -= pos4x;
            sat4vii.position.y += pos4y;

            sat5vii.position.x -= pos5x;
            sat5vii.position.y += pos5y;

            sat6vii.position.x += pos6x;
            sat6vii.position.y -= pos6y;

            sat7vii.position.x += pos7x;
            sat7vii.position.y -= pos7y;

            sat8vii.position.x += pos8x;
            sat8vii.position.y -= pos8y;

            sat9vii.position.x += pos9x;
            sat9vii.position.y += pos9y;

            sat10vii.position.x -= pos10x;
            sat10vii.position.y += pos10y;

            sat11vii.position.x += pos11x;
            sat11vii.position.y -= pos11y;

            sat12vii.position.x += pos12x;
            sat12vii.position.y -= pos12y;

            sat13vii.position.x += pos13x;
            sat13vii.position.y += pos13y;

            sat14vii.position.x -= pos14x;
            sat14vii.position.y += pos14y;

            sat15vii.position.x += pos15x;
            sat15vii.position.y -= pos15y;

            sat16vii.position.x += pos16x;
            sat16vii.position.y += pos16y;
            
            sat17vii.position.x -= pos17x;
            sat17vii.position.y -= pos17y;

            sat18vii.position.x -= pos18x;
            sat18vii.position.y += pos18y;

            sat19vii.position.x -= pos19x;
            sat19vii.position.y -= pos19y;

            sat20vii.position.x += pos20x;
            sat20vii.position.y += pos20y;

            sat21vii.position.x -= pos21x;
            sat21vii.position.y -= pos21y;

            sat22vii.position.x += pos22x;
            sat22vii.position.y += pos22y;

            sat23vii.position.x -= pos23x;
            sat23vii.position.y -= pos23y;

            sat24vii.position.x += pos24x;
            sat24vii.position.y -= pos24y;

            sat25vii.position.x += pos25x;
            sat25vii.position.y -= pos25y;

            sat26vii.position.x += pos26x;
            sat26vii.position.y += pos26y;

            sat27vii.position.x += pos27x;
            sat27vii.position.y += pos27y;

            sat28vii.position.x -= pos28x;
            sat28vii.position.y += pos28y;

            sat29vii.position.x += pos29x;
            sat29vii.position.y -= pos29y;

            sat30vii.position.x += pos30x;
            sat30vii.position.y += pos30y;

            sat1viii.position.x += pos1x;
            sat1viii.position.y -= pos1y;

            sat2viii.position.x -= pos2x;
            sat2viii.position.y += pos2y;

            sat3viii.position.x += pos3x;
            sat3viii.position.y -= pos3y;

            sat4viii.position.x -= pos4x;
            sat4viii.position.y += pos4y;

            sat5viii.position.x -= pos5x;
            sat5viii.position.y += pos5y;

            sat6viii.position.x += pos6x;
            sat6viii.position.y -= pos6y;

            sat7viii.position.x += pos7x;
            sat7viii.position.y -= pos7y;

            sat8viii.position.x += pos8x;
            sat8viii.position.y -= pos8y;

            sat9viii.position.x += pos9x;
            sat9viii.position.y += pos9y;

            sat10viii.position.x -= pos10x;
            sat10viii.position.y += pos10y;

            sat11viii.position.x += pos11x;
            sat11viii.position.y -= pos11y;

            sat12viii.position.x += pos12x;
            sat12viii.position.y -= pos12y;

            sat13viii.position.x += pos13x;
            sat13viii.position.y += pos13y;

            sat14viii.position.x -= pos14x;
            sat14viii.position.y += pos14y;

            sat15viii.position.x += pos15x;
            sat15viii.position.y -= pos15y;

            sat16viii.position.x += pos16x;
            sat16viii.position.y += pos16y;
            
            sat17viii.position.x -= pos17x;
            sat17viii.position.y -= pos17y;

            sat18viii.position.x -= pos18x;
            sat18viii.position.y += pos18y;

            sat19viii.position.x -= pos19x;
            sat19viii.position.y -= pos19y;

            sat20viii.position.x += pos20x;
            sat20viii.position.y += pos20y;

            sat21viii.position.x -= pos21x;
            sat21viii.position.y -= pos21y;

            sat22viii.position.x += pos22x;
            sat22viii.position.y += pos22y;

            sat23viii.position.x -= pos23x;
            sat23viii.position.y -= pos23y;

            sat24viii.position.x += pos24x;
            sat24viii.position.y -= pos24y;

            sat25viii.position.x += pos25x;
            sat25viii.position.y -= pos25y;

            sat26viii.position.x += pos26x;
            sat26viii.position.y += pos26y;

            sat27viii.position.x += pos27x;
            sat27viii.position.y += pos27y;

            sat28viii.position.x -= pos28x;
            sat28viii.position.y += pos28y;

            sat29viii.position.x += pos29x;
            sat29viii.position.y -= pos29y;

            sat30viii.position.x += pos30x;
            sat30viii.position.y += pos30y;

            sat1ix.position.x += pos1x;
            sat1ix.position.y -= pos1y;

            sat2ix.position.x -= pos2x;
            sat2ix.position.y += pos2y;

            sat3ix.position.x += pos3x;
            sat3ix.position.y -= pos3y;

            sat4ix.position.x -= pos4x;
            sat4ix.position.y += pos4y;

            sat5ix.position.x -= pos5x;
            sat5ix.position.y += pos5y;

            sat6ix.position.x += pos6x;
            sat6ix.position.y -= pos6y;

            sat7ix.position.x += pos7x;
            sat7ix.position.y -= pos7y;

            sat8ix.position.x += pos8x;
            sat8ix.position.y -= pos8y;

            sat9ix.position.x += pos9x;
            sat9ix.position.y += pos9y;

            sat10ix.position.x -= pos10x;
            sat10ix.position.y += pos10y;

            sat11ix.position.x += pos11x;
            sat11ix.position.y -= pos11y;

            sat12ix.position.x += pos12x;
            sat12ix.position.y -= pos12y;

            sat13ix.position.x += pos13x;
            sat13ix.position.y += pos13y;

            sat14ix.position.x -= pos14x;
            sat14ix.position.y += pos14y;

            sat15ix.position.x += pos15x;
            sat15ix.position.y -= pos15y;

            sat16ix.position.x += pos16x;
            sat16ix.position.y += pos16y;
            
            sat17ix.position.x -= pos17x;
            sat17ix.position.y -= pos17y;

            sat18ix.position.x -= pos18x;
            sat18ix.position.y += pos18y;

            sat19ix.position.x -= pos19x;
            sat19ix.position.y -= pos19y;

            sat20ix.position.x += pos20x;
            sat20ix.position.y += pos20y;

            sat21ix.position.x -= pos21x;
            sat21ix.position.y -= pos21y;

            sat22ix.position.x += pos22x;
            sat22ix.position.y += pos22y;

            sat23ix.position.x -= pos23x;
            sat23ix.position.y -= pos23y;

            sat24ix.position.x += pos24x;
            sat24ix.position.y -= pos24y;

            sat25ix.position.x += pos25x;
            sat25ix.position.y -= pos25y;

            sat26ix.position.x += pos26x;
            sat26ix.position.y += pos26y;

            sat27ix.position.x += pos27x;
            sat27ix.position.y += pos27y;

            sat28ix.position.x -= pos28x;
            sat28ix.position.y += pos28y;

            sat29ix.position.x += pos29x;
            sat29ix.position.y -= pos29y;

            sat30ix.position.x += pos30x;
            sat30ix.position.y += pos30y;
}